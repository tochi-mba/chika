import pygame, sys\nfrom pygame.locals import *\n\npygame.init()\n\nWIDTH, HEIGHT = 448, 576\nTILE = 16\nWINDOW = pygame.display.set_mode((WIDTH, HEIGHT))\npygame.display.set_caption('Pacman Clone')\n\nclock = pygame.time.Clock()\n\nplayer_img = pygame.Surface((TILE, TILE))\nplayer_img.fill((255, 255, 0))\nplayer_x, player_y = WIDTH//2, HEIGHT//2\n\nspeed = 4\n\nrunning = True\nwhile running:\n    for event in pygame.event.get():\n        if event.type == QUIT:\n            pygame.quit()\n            sys.exit()\n\n    keys = pygame.key.get_pressed()\n    if keys[K_LEFT]: player_x -= speed\n    if keys[K_RIGHT]: player_x += speed\n    if keys[K_UP]: player_y -= speed\n    if keys[K_DOWN]: player_y += speed\n\n    WINDOW.fill((0, 0, 0))\n    WINDOW.blit(player_img, (player_x, player_y))\n    pygame.display.flip()\n    clock.tick(60)