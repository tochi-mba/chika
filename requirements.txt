fastapi>=0.111.0
uvicorn[standard]>=0.29.0
websockets>=12.0
pydantic>=2.7.0
python-dotenv>=1.0.0

# LLM providers (install all; only the chosen one is imported at runtime)
openai>=1.30.0
anthropic>=0.28.0

# Tools
httpx>=0.27.0
duckduckgo-search>=6.1.0
