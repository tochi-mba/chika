{
  "name": "chika-extension",
  "version": "1.0.0",
  "private": true,
  "description": "Chika Browser Agent — Chrome extension. Tests live under e2e/.",
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:install": "playwright install --with-deps chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.45.0"
  }
}
